---
slug: /engine
sidebar_position: 10
title: Engine Guide
sidebar_label: Engine Guide
description: Run the deterministic EATGF compliance engine and understand its input contracts and operational checks.
---

import DocMetaStrip from '@site/src/components/docs/DocMetaStrip';
import DocCardGrid from '@site/src/components/docs/DocCardGrid';
import DocCard from '@site/src/components/docs/DocCard';
import DocSectionHeader from '@site/src/components/docs/DocSectionHeader';
import DocChecklist, { DocChecklistItem } from '@site/src/components/docs/DocChecklist';
import DocCallout from '@site/src/components/docs/DocCallout';
import DocNextSteps from '@site/src/components/docs/DocNextSteps';

<DocMetaStrip items={[
	{ label: 'Engine', value: 'Deterministic EATGF v1.1' },
	{ label: 'Runtime', value: 'Python 3.11+' },
	{ label: 'Output', value: 'Machine-verifiable compliance report' },
]} />

The EATGF Engine is the deterministic, audit-grade compliance engine for the EATGF framework. It evaluates organizational evidence against the canonical registry and produces machine-verifiable compliance reports.

<DocSectionHeader number="01" label="Overview" description="Operational characteristics of the engine used across the v1.1 workflow." />

- Open-source, production-grade compliance engine
- Deterministic: same inputs always yield same outputs
- CLI-based: runs as a command-line tool
- Registry-driven: uses the canonical EATGF registry

<DocSectionHeader number="02" label="How to Run" description="Clone the repository and execute the core engine commands." />

```bash
git clone https://github.com/tariqsaidofficial/eatgf-engine.git
cd eatgf-engine
python -m eatgf_engine.cli.main validate-registry registry_v1.1.json
python -m eatgf_engine.cli.main evaluate-compliance registry_v1.1.json org_profile.json evidence.json --output-json compliance_report.json
```

<DocSectionHeader number="03" label="v1.1 CI Regression Checks" description="Run the same safety checks used to verify zero-evidence and invalid-status handling." />

```bash
python -m eatgf_engine.cli.main evaluate-compliance registry_v1.1.json org_profile.json tests/ci_zero_evidence.json
python -m eatgf_engine.cli.main evaluate-compliance registry_v1.1.json org_profile.json tests/ci_invalid_status.json && exit 1 || exit 0
```

<DocSectionHeader number="04" label="Input Templates" description="Use the following files as the baseline inputs for the engine workflow." />

<DocCardGrid cols={2}>
	<DocCard title="Org Profile Templates" description="docs/templates/org_profile.saas.json · docs/templates/org_profile.onprem.json" />
	<DocCard title="Evidence Templates" description="docs/templates/evidence.blank.json · docs/templates/evidence.example.json" />
</DocCardGrid>

Usage guide: [Evidence Guide](./evidence-guide)

<DocSectionHeader number="05" label="org_profile Contract (v1.1)" description="org_profile.json must contain all required fields with strict allowed values." />

`org_profile.json` must contain all fields below:

- `environment` (allowed values only: `Cloud`, `SaaS`, `On-Prem`, `Hybrid`)
- `ai_usage` (boolean only: `true` or `false`)
- `apis_exposed` (boolean only: `true` or `false`)

Validation is fail-fast. Missing fields or invalid values stop execution with explicit error messages.

<DocSectionHeader number="06" label="evidence_metrics (v1.1)" description="Status of the optional evidence_metrics field in the current release." />

`evidence_metrics` is optional, ignored in v1.1 scoring/reporting, and reserved for v1.2+.

<DocSectionHeader number="07" label="Operational Checklist" description="Confirm the engine run is complete and valid before using the output report." />

<DocChecklist label="Run completion checks">
	<DocChecklistItem>Registry validated successfully</DocChecklistItem>
	<DocChecklistItem>`org_profile.json` contains all required fields</DocChecklistItem>
	<DocChecklistItem>`evidence.json` uses only supported statuses</DocChecklistItem>
	<DocChecklistItem>`compliance_report.json` generated successfully</DocChecklistItem>
	<DocChecklistItem>Regression checks pass for zero evidence and invalid status cases</DocChecklistItem>
</DocChecklist>

<DocCallout type="info" title="Recommended sequence">
Run this page after [Quick Start Guide](./quick-start), then validate evidence quality in [Evidence Guide](./evidence-guide).
</DocCallout>

<DocNextSteps steps={[
	{ label: 'Evidence Guide', href: './evidence-guide', description: 'Validate schema, statuses, and evidence quality before the final run.' },
	{ label: 'Quick Start', href: './quick-start', description: 'Follow the end-to-end operational path for v1.1.' },
	{ label: 'EATGF Engine Repository', href: 'https://github.com/tariqsaidofficial/eatgf-engine', description: 'Source code, release history, and engine implementation details.' },
]} />
